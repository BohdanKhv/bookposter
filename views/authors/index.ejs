<div class="row justify-content-center">
    <div class="col-6">
        <h3>Search Authors</h3>
        <form action="authors" method="GET">
            <label class="form-label">Name</label>
            <input type="text" name="name" class="form-control" value="<%= locals.searchOptions.name %>" />
            <div class="row justify-content-end my-3">
                <input type="submit" value="Search" class="btn btn-primary col-4 mx-3"> 
            </div>
        </form>
    </div>
</div>
<br/>
<table class="table table-striped table-hover align-middle">
    <thead>
        <tr class="table-active">
            <th scope="col">№</th>
            <th scope="col">Author</th>
            <th scope="col" class="text-end">Actions</th>
        </tr>
    </thead>
    <tbody>
        <% authors.forEach((author, index) => { %>
            <tr class="justify-content-center">
                <th scope="row">
                    <%= index + 1 %>
                </th>
                <td>
                    <a href="/authors/<%= author.id %>"><%= author.name %></a>
                </td>
                <td class="text-end">
                    <a href="/authors/<%= author.id %>/edit" class="btn btn-sm text-end btn-warning me-2">Edit</a>
                    <%- include('../partials/deleteButton.ejs', { url: `/authors/${ author.id}` }) %>
                </td>
            </tr>
        <% }) %>
    </tbody>
</table>